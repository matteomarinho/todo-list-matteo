import {ButtonAdd} from './ButtonAdd';

export const List = (props) =>{
    return (
        <p>
        <ButtonAdd/>
        <script>
        if (inputValue === 'cocu') {
            alert("You must write something!")
        }
        </script>
        {props.children}
        </p>
    )
}